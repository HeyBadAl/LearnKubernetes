# Terraform .terraform directory
**/.terraform/

# Terraform .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log

# Exclude all .tfvars files, as they are often used for sensitive data
*.tfvars

# Exclude override files as they are usually used to override default settings
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Include override files you do want committed
!example_override.tf

# Include tfplan files to see the changes between the plan and apply runs
*.tfplan

# Excludes the .retry files created by `terraform apply`
*.retry

# Exclude the .terraformrc file which may contain sensitive variables
.terraformrc
terraform.rc

